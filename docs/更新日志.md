# 更新日志

> 简要记录仓库文档与 Demo 行为的重要变更，便于选手与评测对齐。

## 2026-05-08

- **复赛总时长上限（文档）**：《赛题详情》中复赛**总仿真运行时长**由「不超过 1 小时」调整为「**不超过 4 小时**」。见 `docs/01-赛题详情.md`。
- **参赛约束（文档补充）**：明确禁止决策代码**直接读取**货源 / 司机原始文件（如 `cargo_dataset.jsonl`、`drivers.json`）；货源与司机信息须通过仿真接口（如 `query_cargo`、`get_driver_status`、`query_decision_history` 等）获取。详见 `docs/02-数据说明.md`「1.1 信息获取约束」；`demo/README.md` 文首有简要重述。
- **司机信息与偏好**：更新 `demo/server/data/drivers.json`（及部分裁判侧同步数据）：调整多名司机的偏好条款（文案、罚金额度与封顶、窗口时间等），含**新增或改写**的规则。
- **偏好评测脚本**：`demo/calc_monthly_income.py` 已按上述司机配置重做各 `DriverDxxx` 偏好计算逻辑，罚分与明细字段与 JSON 中 `penalty_amount` / `penalty_cap` 对齐。
- **决策历史查询**：在 `SimulationApiPort` 中增加 `query_decision_history(driver_id, step)`。评测运行时，历史来自与仿真编排器**共享的内存列表**（与落盘 `actions_*.jsonl` 每条记录结构一致），`step=-1` 为本司机本会话全部步骤，正整数为末尾若干条（查看上一笔传 `1`）。

## 2026-05-12

- **新增收益优先策略**：增加 `new_release_profit_guarded_agent`，保留 D009 熟货、D010 家事跨天、D002/D008 休息等正收益保护，撤掉 D001/D004 的过度保守规则，并固化 D003/D004 的 `0.02 * estimated_net`、D005 的 `0.03 * estimated_net` 定向收益权重。30 天本地回测净收益 `261512.57`、偏好罚分 `16365.0`、失败司机 `0`。
- **修复熟货锁单口径**：`simkit.take_order` 改为接单入口锁定货源，避免 `remove_time` 早于 `load_time` 的货源在等待装货期间被同步下架，导致已发起接单最终误判失败。
- **记录负实验**：D006 强制 5 小时休息会把罚分从 `16965.0` 降到 `11365.0`，但总净收益从 `260089.27` 降到 `256326.84`，当前不作为默认策略。
- **记录负实验**：全局净收益权重 `AGENT_SCORE_NET_WEIGHT=0.02` 退化到 `248682.56`；D009 熟货后放松回家约束退化到 `255542.75` 且罚分爆到 `41265.0`；D010 强制 3 小时休息退化到 `259698.24`。结论是只能采用司机定向收益权重，不能全局追高毛利或硬控休息。

## 2026-05-14

- **D004 午休/首单 tradeoff 粗网格**：新增 `demo/run_d004_grid.py`，用于批量搜索 D004 的 `AGENT_AP_D004_LUNCH_FIRST_MIN_NET` 与 `AGENT_AP_D004_LUNCH_FIRST_MIN_NPH`。`620:50` 至 `680:50` 均达到当前最好 `283234.33`；`560:45`、`600:45` 退化到 `282875.50`。结论是 D004 不能单纯降低阈值，下一步应引入后续路径价值判断。
- **D004 边界网格**：`600-680:48` 退化到 `282875.50`，`700-740` 相关组合稳定在 `283134.33`，只有 `680:50`、`680:52` 达到 `283234.33`。D004 阈值搜索已饱和，默认锁定 `680:50`，后续主线转向 D009/D010/D006 的独立规则和 D004 的路径价值算法。
- **D009/D010 探索更新**：D009 唯一 `900` 夜间/回家罚由必接熟货 `240646` 跨夜导致，不应继续强修。新增 `demo/run_d010_rest_grid.py` 与 D010 机会成本补休开关，用于测试 `3/9、3/18、3/24、3/27、3/29` 五个休息违规日是否值得单日修复。
- **多司机机会池**：明确后续按司机独立 delta 并行分析，D004 固定当前最优，D009 不主攻；优先同时探索 D010 单日补休、D001 低机会成本 8 小时休息、D006 低 NPH 5 小时休息、D008 食品饮料软罚规避。
- **Multi-driver rest grid**：新增 `demo/run_multi_rest_grid.py`，支持同一批实验中同时启用 D001/D006/D010 的机会成本补休配置，例如 `D001:5+D006:26+D010:9`，用于快速筛选可相加的独立正收益规则。
- **Multi-driver rest 首轮结果**：`D001:16` 达到 `283873.17`，相对当前最好 `+638.84`；`D001:17` 提升 `+300.12`；`D010:29` 提升 `+12.69`。下一轮聚焦 D001 16 日补休与 D010 29 日组合。
- **新最好结果**：`D001:16 + D010:29` 达到 `283885.86`，相对 `283234.33` 提升 `+651.53`。`D001:16,17` 没有额外收益，`D001:16,22/23` 明显退化。新结果已写入 `demo/agent_memory.json`。
- **新最好结果更新**：`D001:16 + D010:27,29` 达到 `283962.97`，相对 `283234.33` 提升 `+728.64`，偏好罚分降到 `13765.0`。该结果作为离线 oracle 信号，不作为最终提交的写死日期方案。
- **算法层 Agent 升级**：`new_release_agentic_planner_agent` 新增 `visible_chain_value` 与 `shadow_price_rest`。前者评估当前单完成后是否能赶上可见后继货源的装货窗/下架时间，后者按司机画像比较休息罚分影子价格与当前可见订单机会成本。
- **新增算法实验入口**：增加 `demo/run_agentic_algo_grid.py`，用于无日期补丁地批量测试 `chain_d001`、`chain_core`、`shadow_d001_balanced`、`chain_shadow_d001_d010`、`wide_query_chain_shadow` 等在线算法画像。
- **工具层下一步更新**：`demo/competition_ops.py next` 已从单日修复建议切换为“链路价值 + 影子价格休息 + gated LLM rerank”的算法主线。
- **agentic_algo_v1 结果**：`shadow_d001_balanced` 与 baseline 同分 `283234.33`，说明休息影子价格阈值未触发；`chain_d001` 总分退化到 `275856.09`，但 D001 自身从 `16224.17` 升到 `17414.81`，说明链路价值方向对 D001 有信号，但默认权重误影响了其他司机。
- **链路价值修正**：`visible_chain_value` 默认权重改为 `0.0`，仅对显式配置 `AGENT_AP_Dxxx_CHAIN_WEIGHT` 的司机生效；`run_agentic_algo_grid.py` 增加 `chain_d001_tiny/small`、`chain_d004_small`、`chain_d010_small`、`shadow_d001_loose/very_loose` 等消融预设，避免过早否定方法。
- **agentic_ablation_v2 新算法最好**：`chain_d001` 达到 `284424.97`，相对 `283234.33` 提升 `+1190.64`，并超过离线日期补丁 `283962.97`。当前主线改为 D001 链路价值权重搜索。
- **消融结论**：`chain_d010_small` 正收益 `+525.33`，可尝试与 D001 组合；`shadow_d001_very_loose` 小正收益但低于链路主线；`chain_d004_small` 与 `shadow_d010_loose` 明显负收益，暂不继续投入。
- **chain_combo_v3 新最好**：`chain_d001_d010_w10` 与 `chain_d001_d010_w15` 达到 `284950.30`，相对 `283234.33` 提升 `+1715.97`，相对离线日期补丁 `283962.97` 提升 `+987.33`。D001 与 D010 的可见后继货源链路价值可以叠加。
- **当前算法结论**：主线确认是链路价值而非休息补罚；D001 chain weight `0.70` 搭配 D010 chain weight `0.10/0.15` 为当前最好方案。D001 shadow 组合降到 `282245.96/282771.29`，明确不并入最终策略。
- **chain_refine_v4 新最好**：`chain_d001w11_d010w10` 与 `chain_d001w11_d010w15` 达到 `285277.89`，相对 `283234.33` 提升 `+2043.56`，相对离线日期补丁 `283962.97` 提升 `+1314.92`。D001 最优链路权重附近为 `1.10`，D010 为 `0.10/0.15`。
- **并行探索结论**：扩大查询视野 `D001=400`、`D010=300`、`both wide` 均退化，说明扫描时间和远端候选噪声会抵消链路价值收益。后续锁定当前视野，做 `D001 1.00-1.15` 与 `D010 0.08-0.15` 窄范围精搜。
- **全司机画像探索**：`run_agentic_algo_grid.py` 汇总列扩展到 D001-D010 全部司机，后续按“单司机信号发现 -> 叠加到 `D001=1.10 + D010=0.10` 当前最优基线复测”的方式探索独立策略。新增/使用 `chain_d002_small`、`chain_d003_small`、`chain_d005_small`、`chain_d006_small`、`chain_d007_small`、`chain_d008_small`、`chain_d009_small` 与 `best_dxxx_chain` 系列预设，避免只围绕 D004 或只看总分。
- **实验工具修复**：23:05 多批并行结果出现无关预设同分高分，判断存在共享 `demo/results` 输出目录串扰和终端 `AGENT_*` 环境继承风险。`run_agentic_algo_grid.py` 已改为每个 preset 独立 `raw_results`、独立 config、独立评分，并在子进程环境中清理残留 `AGENT_*`。23:05 批次只作为启发，需用 `safe_refine_v6`、`safe_driver_addon_v6`、`safe_profile_rules_v6` 重跑验证。
- **safe_hot_v6 安全结果**：旧的 `chain_d001w10_d010w10=285994.15` 未复现，安全重跑为 `284906.05`；`D004 strict_quota` 也无独立收益。真实新增信号来自 `D007 chain` 与 `D009 chain`，二者在 `D001=1.00 + D010=0.10` 上可叠加到 `286371.21`。下一步转为 `D001=1.05/1.10 + D010=0.10 + D007/D009` 高分细搜。
- **v7 全量发现**：当前安全最好为 `hot_d001w105_d010w10_d007w10_d009w12=286779.91`。除最高分外，结论是：D001 存在 `1.05/1.10` plateau，D007/D009 chain 是可叠加主收益，D005 有小正信号，D003 chain 有弱正信号；D002 chain/扩视野、D008 chain、D006 chain、D009 home slack、D003 aftercap、D004 strict 均为负收益或无收益。后续停止负方向，优先探索当前主线叠加 D005/D003。
- **v10 新最好**：`hot_full_d003w05_d005w08_d009limit220` 达到 `289463.55`，偏好罚分 `14365.0`。新高来自 `D003 w0.05 + D005 w0.08 + D007/D009 chain + D009 候选视野 220` 的组合；D009 limit 呈非单调甜点，`220` 最好，`250` 开始增罚降分，旧的 `288323.21` 不再是当前最好。D006 tiny 在 full default 上有 `+144.63` 小正信号，下一步需叠加到 `D009 limit 220` 底座验证。
- **v11 新最好**：`hot_full_d003w05_d005w08_d009limit220_d009w10` 达到 `289978.86`，偏好罚分仍为 `14365.0`。新收益来自 D009 净收益提升到 `19196.56`；`D009 w0.10` 优于 `w0.08` 与 `w0.14+`，说明 limit220 下链路价值需要适中而不是越强越好。D006 tiny 可在旧底座上增加 `+144.63`，下一步优先测试 `D009 limit220 + w0.10 + D006 tiny` 冲击 `290000+`。
- **v12 新最好**：`hot_best_d009w10_limit220_d006w015` 达到 `290123.49`，偏好罚分仍为 `14365.0`。新增收益来自 D006 净收益从 `33708.48` 到 `33853.11`，与 D009 新高完全可叠加。D009 最优区间进一步确认：`w0.09/w0.095/w0.10 + limit220` 同分，`limit218` 低 `180.69`，`limit222+` 多 `900` 罚分。下一步只细测 D009 `219/220/221` 边界、D006 视野，以及 D009 罚分动作差分。
- **v13 新最好**：`hot_v13_d006limit200` 达到 `291935.15`，偏好罚分仍为 `14365.0`。新高完全来自 D006 候选视野收缩到 `200` 后净收益提升到 `35664.77`；D006 limit 呈强非单调，`350` 只有 `290212.35`，`250/400/500/150` 均明显退化。D009 `219/220/221` 边界和风险门控确认不是下一步主增量，后续聚焦 D006 limit `170-230` 精搜与 D006 chain 触发阈值。
- **v14 新最好**：`hot_v14_d010limit200` 达到 `293097.13`，偏好罚分 `14765.0`。新正信号来自多个司机独立 query limit：D010 limit200 `+1161.98`、D004 limit600 `+819.90`、D007 limit400 `+683.65`、D003 limit200 `+195.52`、D006 limit202 `+7.59`。D001/D002/D005/D008 的视野搜索暂未发现正收益。下一步做这些正信号的组合叠加验证。
- **v15 新最好**：`hot_v15_all_d006202` 达到 `294803.79`，偏好罚分 `14965.0`。`D003 limit200 + D004 limit600 + D006 limit202 + D007 limit400 + D010 limit200` 基本可叠加，相对 v14 提交版提升 `+1706.66`。下一步更新提交 profile，并在全量组合上继续细测 `D004 600/620`、`D007 400/420`、`D006 200/202`。
- **覆盖恢复记录**：误将 `demo_docs_release_20260509.zip` 解压到 0508 目录后，`model_decision_service.py` 与 `embedded_agent.py` 被官方模板覆盖，导致 v16 实验回退到每步 LLM 调用并报 `401 Unauthorized`，且每司机独立 query limit 不再生效。已恢复策略调度器、`AGENT_D001..D010_NEAREST_CARGO_LIMIT` 支持，并将默认提交 profile 更新为 `v15_all_d006202_294803`。当前 `demo/server/data/cargo_dataset.jsonl` 是 0509 数据，若要继续 0508 对比，需要从 0508 zip 恢复数据文件。
- **0509 迁移验证**：建立独立目录 `/home/zrr/study/demo_docs_release_20260509` 后，0508 最好 `hot_v15_all_d006202` 在 0509 数据上复现 `294803.79 / penalty 14965`。`D006 limit202`、`D009 limit220`、`D004 limit600`、多司机 query-limit 组合等规律稳定迁移；`D002/D008/D006 chain` 与 D009 硬回家/余量规则仍为负方向。新增值得验证的信号是 `D007 limit420` 局部优于 `400`，下一轮应在全量组合上复测 `D007 410/420/430/440`。
- **0509 新最好**：v16 精搜确认 `D007 limit410/420` 可叠加到全量组合，`hot_v16_all_d007410`、`hot_v16_all_d007420`、`hot_v16_all_d004600_d007420_d006202` 均达到 `295097.91 / penalty 14965`，相对 `hot_v15_all_d006202` 提升 `+294.12`，且唯一增量来自 D007 净收益。D004 仍以 `600` 最好，D006 `201/202` 同分最好，D009 `220` 稳定，D003/D010 默认值不变。下一步应从参数精搜转向动作级 regret 诊断和短视野路径规划。
- **0509 v17 平台期确认**：8 组 50 个候选未超过 `295097.91 / penalty 14965`。`D007 410/415/420` 同分，`425+` 退化；`D004 600`、`D006 201/202`、`D009 220` 均确认是尖峰或平台边界；`D001/D010`、`D003/D005`、`D004 lunch` 没有新增可叠加收益。结论是固定司机参数已进入平台期，后续主线应转向 action-level regret、短视野 beam/rollout 与司机状态自适应候选池，而不是继续只调常数。
- **v18 短视野规划器**：在 0509 agent 中新增 `AGENT_AP_ENABLE_TWO_STEP_ROLLOUT`，基于在线可见货源模拟“当前单完成后的后继单价值、机会密度、司机状态风险”，形成 `base + chain + rollout` 的司机独立小规划器；同时新增 `hot_v18_rollout_*` preset，支持单司机归因和组合验证。该实现不读取原始数据文件，符合赛题在线决策约束。
- **算法设计汇总**：新增 `docs/agent_algorithm_design.md`，把 0508/0509 探索发现汇总成可执行的四层在线决策架构：hard filter、immediate value、chain/rollout value、driver state risk，并给出 D001-D010 的司机画像与默认策略边界。
- **v18 首轮结果**：小规划器未超过 `295097.91`。`D001 rollout` 与基线同分，`D006 -232.06`、`D009 -265.31`、`D004 -893.63`、`D007 -925.78`、`D010 -1799.25`；`core_light -2700.01`、`core -4116.03`、`core_strong -6436.41`。结论是通用 rollout 会过度改动作，下一步应改为 gated top-k rerank，只在 base 分差小的场景介入，并暂停 D004/D007/D010 的通用 rollout。
- **v19 gated rollout**：已实现 `AGENT_AP_ENABLE_GATED_ROLLOUT`。新逻辑先用 v17 base score 排序，只在 top1/top2 分差小于阈值时，让 rollout 在 top-k 近邻候选内加有限 bonus；默认只对白名单 `D001,D006,D009` 开启，D007/D010 仅 tiny probe。新增 `hot_v19_gated_*` preset 用于验证小规划器作为安全重排器是否有效。
- **v19 新最好**：`hot_v19_gated_d007_d010_tiny` 达到 `295352.70 / penalty 14665`，相对 v17 最好 `+254.79`。增量来自 `D010 tiny gate +235.41` 且罚分少 `300`，以及 `D007 tiny gate +19.38`；二者可叠加。D001/D006 gated 同分无收益，D009 gated 稳定退化 `-265.31`，后续应移出白名单。结论是小规划器应作为极窄 top-k rerank，而不是全局 rollout。
- **v20 细搜结论**：围绕 D007/D010 tiny gate 的 gap、cap、top-k、base-drop 与 query-limit 细搜未超过 `295352.70`，但确认新高稳定。D010 单独 `295333.32`，D007 单独 `295117.29`，组合 `295352.70`；`top5` 退化，D007 limit 必须保持 `420`，D010 limit `195/200` 同分、`205` 退化。下一步应做 D007/D010 动作级差分诊断，而不是继续细扫 gap/cap。
- **v21 负实验与 v22 修正**：D010 “夜间短单保休息”泛化失败，`hot_v21_d010night_*` 退化到 `293450.88 / penalty 14965`，相对 v20 最好低 `1901.82`。动作差分显示 v20 的正收益来自 step22：base 首选长单会破坏次日早晨前的回家/目标地休息恢复；v21 的负收益来自 step87：base 首选单本来就能恢复休息，短单偏好反而改坏后续链路。已新增 `AGENT_AP_ENABLE_D010_RECOVERY_GATE` 与 `hot_v22_recovery_*`，只在 base 首选单无法恢复休息且替代候选能恢复时才介入。

