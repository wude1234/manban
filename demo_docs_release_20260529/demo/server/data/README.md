# Data Directory

Place the official competition data files here before running local simulations:

```text
cargo_dataset.jsonl
drivers.json
```

These raw data files are intentionally not committed to this repository. The competition evaluation environment provides its own data, and the submitted ZIP should not include `demo/server/data/`.
